#!/bin/env bash
. /home/paczur/System/Scripts/system/path
. ~/.bashrc
export PATH
t="$(tty | sed 's/.*\/tty//g')"
case "$t" in
  "1")
  wm="/usr/bin/awm"
  ;;
  *)
  wm=""
  ;;
esac
set bell_style none
if [ -n "$wm" ]; then
  exec startx ~/.xinitrc "$wm" -- "vt$t"
fi
clear
